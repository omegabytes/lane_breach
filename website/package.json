{
  "name": "website",
  "version": "1.0.0",
  "description": "Hookup an AWS API Gateway endpoint to a Lambda function to render HTML on a `GET` request",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^4.1.3"
  }
}
